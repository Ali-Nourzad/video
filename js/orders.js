const durationMap={"30":"حدود ۳۰ ثانیه","60":"حدود ۱ دقیقه","120":"حدود ۲ دقیقه","180":"حدود ۳ دقیقه","300":"حدود ۵ دقیقه","600":"حدود ۱۰ دقیقه",custom:"بیشتر از ۱۰ دقیقه"};
async function uploadFile(orderId,userId,file){const name=file.name.replace(/[^\w\u0600-\u06FF.\- ]/g,"_");const path=`${userId}/${orderId}/${crypto.randomUUID()}-${name}`;const up=await supabase.storage.from("video-files").upload(path,file,{upsert:false});if(up.error)return up.error.message;const url=supabase.storage.from("video-files").getPublicUrl(path).data.publicUrl;const ins=await supabase.from("order_files").insert({order_id:orderId,uploaded_by:userId,file_name:file.name,file_path:path,file_url:url,file_size:file.size,mime_type:file.type||"application/octet-stream"});return ins.error?ins.error.message:null}
async function createOrder(x){const{data:order,error}=await supabase.from("video_orders").insert({customer_id:x.userId,title:x.title,subject:x.subject,description:x.description,estimated_duration:durationMap[x.duration]||x.duration,production_model:x.model,aspect_ratio:x.aspectRatio,output_quality:x.quality,video_style:x.style,reference_links:x.links,special_notes:x.notes,status:"new"}).select().single();if(error)return{ok:false,error:error.message};for(const f of Array.from(x.files||[])){const e=await uploadFile(order.id,x.userId,f);if(e)return{ok:false,error:`سفارش ثبت شد اما فایل ${f.name} ارسال نشد: ${e}`}}return{ok:true,order}}
